version: "3.9"

services:
  backend:
    build:
      context: ./backend

    container_name: news_backend

    ports:
      - "8000:8000"

    env_file:
      - ./backend/.env

    extra_hosts:
      - "host.docker.internal:host-gateway"

    restart: unless-stopped